virtualenv --system-site-packages .

mkdir -p ./data/attach
mkdir -p ./run
ln -sf ./run/socket ./socket    # compatibility with dev.rexdb.us

./bin/pip --isolated install -e src/rex.setup/
./bin/pip --isolated install -r requirements.txt

