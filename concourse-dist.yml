platform: linux

image_resource:
  type: registry-image
  source:
    repository: vito/oci-build-task
    tag: 0.1.0

inputs:
- name: codebase

outputs:
- name: image

caches:
- path: cache

params:
  CONTEXT: codebase

run:
  path: build
