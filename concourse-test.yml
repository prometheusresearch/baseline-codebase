platform: linux

image_resource:
  type: docker-image
  source:
    repository: rexdb/ci
    tag: 2019.02.04

inputs:
- name: codebase

run:
  path: /bin/sh
  args:
  - -exc
  - |
    service postgresql start
    make init-local
    make test
  dir: codebase
