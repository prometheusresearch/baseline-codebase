name: rex_formbuilder_demo.core
---
revision: 20140828
install:
  - cmd: merge
    aspect: meta:user_group
    data:
      - id(): test
  - cmd: merge
    aspect: meta:user
    data:
      - id(): test.testing

