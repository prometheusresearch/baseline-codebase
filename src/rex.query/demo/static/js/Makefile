BIN = ./node_modules/.bin

install:
	npm install

start:
	@$(BIN)/react-scripts start

test:
	@$(BIN)/react-scripts test

test-cov:
	@$(BIN)/react-scripts test --coverage

ci:
	@$(BIN)/react-scripts test --watch

check:
	@$(BIN)/flow

build:
	@$(BIN)/react-scripts build
