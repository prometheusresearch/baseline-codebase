paths:

  /data/todo:
    port:
      entity: todo

  /data/todo/list:
    port:
      entity: todo
      filters:
      - mode($mode) := switch($mode, 'all', true, 'completed', completed, 'todo', !completed)
